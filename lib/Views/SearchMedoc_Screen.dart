import 'package:flutter/material.dart';


class SearchMedoc extends StatelessWidget {
  const SearchMedoc({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}